HOST=my-machine.abc.viam.cloud
API_KEY_ID=
API_KEY=
ARM_NAME=arm
# Optional — omit if there's no separate gripper component.
GRIPPER_NAME=
PORT=8080

# How long (ms) to hold each waypoint pose before moving to the next one,
# for waypoints that don't specify their own holdMs.
HOLD_MS=400
